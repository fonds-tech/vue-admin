## 1. Implementation

- [x] 1.1 读取 `settings` 中的过渡配置并设计过渡绑定方式（不破坏 keep-alive 缓存）
- [x] 1.2 在 `fd-view` 中接入过渡组件并绑定路由视图
- [x] 1.3 新建 `src/styles/transition.scss`，补充更丰富的淡入淡出/滑动/缩放过渡效果与类名约定
- [x] 1.4 定义统一默认过渡时长与缓动曲线并在样式中应用
- [x] 1.5 在 `src/styles/index.scss` 引入 `transition.scss`
- [x] 1.6 增加“无动画”分支（与设置项联动）
